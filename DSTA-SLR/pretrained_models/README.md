# Pretrained Models

Place optional pretrained checkpoints in this directory.

For public code releases, prefer distributing large `.pt` or `.pth` files through GitHub Releases, cloud storage, or a model hub rather than regular Git history.
