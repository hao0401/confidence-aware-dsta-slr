# Data Tools

Utilities for preparing datasets, validating preprocessed data, and generating experiment configs.

Use the top-level `scripts/*.py` wrappers if you want compatibility with the old command paths.
