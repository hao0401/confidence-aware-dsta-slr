# Common Helpers

Shared script internals live here.

- `experiment_specs.py`: dataset and stream metadata.
- `python_locator.py`: Python interpreter resolution.
- `script_utils.py`: shared runtime and file helpers.
