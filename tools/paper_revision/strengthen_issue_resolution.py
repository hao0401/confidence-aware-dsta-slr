from pathlib import Path

from docx import Document


DOCX_PATH = Path(r"C:/Users/haoha/Desktop/ieee_chu_submission_final_wacv_cn_submission.docx")


UPDATES = {
    121: "综合标准测试比较、统一主干消融、固定平均融合控制、受控扰动对比和开销分析，本文最重要的结论并不是“提出了一个更强的骨架主干”，而是证明了观测可靠性本身可以成为骨架 ISLR 中值得单独研究的设计维度。如果仅按标准测试排名衡量，本文并未在所有基准上稳定超过 DSTA-SLR [7] 等强基线；但从问题驱动与证据完整性的角度看，本文仍具有明确价值：它识别了真实部署中重要但文献中相对欠缺的问题，并在几乎不增加推理开销的前提下，通过统一协议提供了可归因的鲁棒性收益证据。进一步地，在同一代码路径下的置信度信号有效性实验中，original / constant / shuffled / removed confidence 的四流 Top-1 分别为 52.35%、46.14%、47.32% 和 41.78%，说明收益不能简单归因于“多一个输入通道”；与公开 DSTA-SLR 强基线在相同扰动协议下的对比也表明，本文结构增强版本在 clean 条件和强噪声条件下并不占优，但在 10%、20% 和 30% 缺失场景下分别取得 +8.89、+27.68 和 +34.06 个百分点的优势。因此，本文更合理的定位是现有强骨架主干的低开销、可插拔鲁棒性增强组件。",
    123: "尽管上述结果支持本文在统一受控扰动协议下的鲁棒性优势，但仍需明确其适用边界。当前关键点缺失和高斯噪声实验属于受控扰动分析，并不等同于真实部署中的全部退化分布；本文也依赖姿态估计器输出的置信度通道，且该通道在当前管线中仅做清洗与裁剪而未进行额外校准，因此尚未验证跨姿态估计器和跨置信度分布条件下的一致泛化。此外，若前端姿态估计器以高置信度输出系统性误检，当前框架也无法独立校验该置信度的真伪。需要进一步说明的是，本文关于置信度有效性的直接证据来自同一代码路径下的短周期对照实验，而关于真实部署价值的更强证据主要来自低质量样本分析与公开强基线对比，因此这些结论应理解为“对可靠性建模有效性的强支持”，而非对全部真实场景的最终定论。",
    125: "本文围绕骨架孤立词手语识别中长期被弱化的“观测可靠性”问题，提出了关键点置信度引导的统一可靠性建模框架。本文并不试图提出更大的骨架主干，而是将关键点置信度视为相对可靠性先验，并将其系统融入输入编码、空间传播、时间修复、多流融合和训练阶段的一致性约束，使模型能够在识别链路的多个环节显式抑制不可靠观测的负面影响。实验结果表明，所提方法在标准测试条件下保持了具有竞争力的性能；更重要的是，在统一主干与统一受控扰动协议下，该方法在关键点缺失和坐标噪声条件下表现出显著更强的稳定性，同时几乎不增加额外部署开销。结合表 4、表 5 和表 8 以及附加的置信度信号有效性实验与公开强基线对比可以看出，结构可靠性建模主要提升缺失型退化下的稳定性，而一致性训练进一步补足强噪声场景，这也进一步支撑了本文作为低开销、可插拔鲁棒性增强模块的定位。未来工作可进一步结合真实低质量样本、跨估计器设置、重复实验统计以及更显式的不确定性校准策略，对骨架手语识别中的可靠性建模进行更全面的研究。",
}


def main():
    doc = Document(str(DOCX_PATH))
    for idx, text in UPDATES.items():
        para = doc.paragraphs[idx - 1]
        if para.runs:
            para.runs[0].text = text
            for run in para.runs[1:]:
                run.text = ""
        else:
            para.text = text
    doc.save(str(DOCX_PATH))
    print(DOCX_PATH)


if __name__ == "__main__":
    main()
