from pathlib import Path
import argparse

from docx import Document


DEFAULT_INPUT = Path(r"C:/Users/haoha/Desktop/ieee_chu_submission_final.docx")
DEFAULT_OUTPUT = DEFAULT_INPUT.with_name(DEFAULT_INPUT.stem + "_wacv_cn_cleaned.docx")


PARAGRAPH_UPDATES = {
    5: "摘要：骨架孤立词手语识别因表示紧凑、背景干扰小和部署友好而具有吸引力，但其性能对姿态估计误差高度敏感。现有骨架方法主要在干净测试条件下提升时空表示能力，并普遍默认不同关键点与不同时刻的观测近似同等可靠，因此对退化观测下的鲁棒性关注不足。本文提出关键点置信度引导的统一可靠性建模框架，将关键点置信度转化为贯穿识别链路的可靠性先验，并系统引入输入编码、动态图传播、时序校正、多流融合和一致性训练。实验结果表明，所提方法在标准测试条件下保持了具有竞争力的性能；更重要的是，在统一主干与统一受控扰动协议下，该方法显著提升了关键点缺失和坐标噪声条件下的识别稳定性，同时仅带来极小的参数量与 FLOPs 开销。本文的核心价值不在于提出更大的骨架主干，而在于证明显式可靠性建模能够作为现有强骨架识别器的一种低开销、可插拔且具有明确归因证据的鲁棒性增强路径。",
    6: "关键词：孤立词手语识别；骨架表示；关键点置信度；可靠性建模；退化观测鲁棒性",
    9: "孤立词手语识别（isolated sign language recognition, ISLR）面向已经切分好的词级手语片段，在无障碍交流、人机交互与轻量化部署等场景中具有明确应用价值。近年来，随着大规模手语数据集与深度模型的发展，基于视觉表征的手语识别取得了显著进展。其中，骨架表示因其结构紧凑、对背景变化不敏感、计算代价相对较低而受到持续关注。与直接处理 RGB 视频的方法相比，骨架方法更容易部署到资源受限环境，也更适合构建低延迟的词级识别系统。",
    10: "然而，骨架孤立词手语识别的性能高度依赖前端姿态估计质量。手语语义高度依赖双手、手腕、肘部及上肢之间的细粒度协同，当存在遮挡、运动模糊、视角变化或姿态估计失败时，关键点往往会出现抖动、偏移、局部缺失甚至误检。这些误差不仅直接污染输入表示，还会在后续图传播与时间聚合过程中被放大，最终导致识别性能显著下降。相比标准测试条件下的干净骨架，真实部署中的骨架观测往往具有明显的非均匀可靠性，不同关键点、不同时间位置对于最终判别的可信程度并不相同。",
    11: "现有骨架 ISLR 方法主要围绕标准测试条件下的表示能力展开，重点关注更强的时空主干、更长程的依赖建模或更有效的多流特征融合，例如 Pose-BERT [3]、SPOTER [5]、SLGTformer [6]、DSTA-SLR [7]、NLA-SLR [20] 和 P3D [21] 等。这一主线回答的核心问题是“如何提升干净评测协议下的识别精度”，而较少显式讨论“当骨架观测本身质量不稳定时，如何提升识别稳定性”。尤其是在骨架预处理阶段通常会同步输出关键点置信度的前提下，这一信息在识别阶段往往只被弱化为附加通道，甚至被完全忽略。",
    12: "本文认为，关键点置信度不应仅被视为姿态估计过程的附带输出，而应被进一步提升为识别阶段可利用的相对可靠性先验。需要强调的是，本文并不将置信度解释为经过严格校准的正确率概率，而是将其视为一种可用于排序和调节传播强度的软线索：高置信度观测通常更值得保留，低置信度观测则更可能在后续建模中传播噪声。如果这一差异在识别链路中被统一忽略，那么即使主干本身足够强，也仍可能持续受到不可靠观测的干扰。",
    13: "基于这一认识，本文关注一个与“继续增大骨架主干容量”不同但互补的问题：在不显著增加推理开销的前提下，能否将关键点置信度系统性地转化为退化骨架观测下的鲁棒性收益。为此，本文提出一个关键点置信度引导的统一可靠性建模框架，将置信度作为贯穿输入表示、空间传播、时间修复、决策融合与训练约束的统一先验，而不是将其局部地用作单点加权或简单拼接。该框架并不试图替代现有强骨架主干，而是将自身定位为一种面向退化观测的低开销、可插拔鲁棒性增强模块。本文的主要贡献如下。",
    14: "1. 本文将关键点可靠性建模明确提出为骨架孤立词手语识别中的独立设计维度，并将其与已有工作中以提升主干表示能力、长程时序建模能力为主的研究路线区分开来。相比继续增大模型容量，本文关注的是如何在退化骨架观测条件下提升识别系统的稳定性与可部署性。",
    15: "2. 本文提出一个关键点置信度引导的统一可靠性建模框架，将关键点置信度从姿态估计阶段的附带输出转化为识别阶段可利用的统一可靠性先验，并系统贯穿于输入编码、动态图传播、时序校正、多流融合和一致性训练等环节，而不是仅将置信度作为附加输入通道或单一加权因子使用。",
    16: "3. 在统一主干、统一预处理与统一受控扰动协议下，本文通过标准测试比较、模块消融、结构与训练解耦实验、固定融合控制和开销分析，表明所提方法能够在保持竞争性标准性能的同时，以几乎可忽略的参数量与 FLOPs 增量显著提升关键点缺失和坐标噪声条件下的鲁棒性，从而验证了可靠性建模作为强骨架识别器可插拔增强组件的有效性。",
    19: "骨架孤立词手语识别的主要研究脉络集中于如何提升标准测试条件下的表示能力，包括基于图卷积或时序卷积的局部依赖建模，以及利用 Transformer、跨模态先验或部位级建模增强长程上下文。总体而言，现有方法更多关注“如何提升 clean-set 精度”，而较少把输入观测质量本身视为需要单独建模的问题。对于词级手语任务而言，这一缺口尤为关键，因为判别信息高度依赖手部与上肢的细粒度协同，局部关键点误差往往会直接转化为语义混淆。",
    21: "与图结构设计和时间建模相比，显式的关键点可靠性建模在骨架手语识别中仍然相对缺失。在一般人体动作识别和姿态估计领域，已有工作尝试通过鲁棒表征、不确定性估计或跨视角约束缓解关键点抖动、遮挡和误检 [17,18]；但在词级手语识别中，关键点置信度通常仍被视为姿态估计阶段的附带输出，而未被系统性纳入识别链路。本文与此不同，不将置信度作为校准概率解释，而是将其视为可用于调节表示、传播和修复过程的相对可靠性先验。",
    22: "这一差异在手语任务中尤为重要，因为手语语义高度依赖双手与上肢的细粒度协同，局部关键点误差更容易直接转化为判别错误。因此，本文的目标并不是再增加一个更大的骨架主干，而是把关键点可靠性提升为贯穿编码、传播、修复和决策的统一先验。",
    23: "与现有强骨架基线的关系",
    24: "本文采用 DSTA-SLR [7] 风格的强骨架框架作为实验主干，但研究目标与其并不相同。DSTA-SLR [7] 的核心贡献在于提升标准测试条件下的骨架表示能力；本文则固定这一类强主干，并研究一个与其正交的问题：当输入观测可靠性不均匀时，如何让既有空间时序建模过程更加稳定。",
    25: "因此，本文的目标不是替代现有强主干，而是为其提供一个面向退化观测的低开销鲁棒性增强模块。围绕这一定位，本文将关键点置信度统一引入输入表示、图传播、时间修复、融合决策与训练目标之中，使可靠性建模成为与主干能力提升互补的独立研究维度。",
    28: "设输入骨架序列为 X，其中 T 表示采样帧数，V 表示关键点数。每个关键点均由二维坐标及其对应的关键点置信度构成。本文仅将置信度视为相对可靠性排序信号，而非校准后的正确率概率；其作用在于调节后续表示学习、图传播与时间修复过程。与实际实现一致，本文在四流骨架识别框架上建模，分别使用关节点流、骨骼流、关节运动流和骨骼运动流作为输入，并通过质量感知融合得到最终类别预测。",
    29: "从整体流程看，本文框架由质量感知表示学习与可靠性感知训练两部分组成。在前向过程中，模型首先通过置信度引导节点编码构造初始表示；随后利用置信度约束动态图模块完成质量感知的空间传播，并通过可靠性感知时序校正缓解局部时间失真；最后依据不同流的样本级质量估计执行动态融合。在训练阶段，本文进一步引入可靠性感知一致性约束，以提升原始视图与退化视图之间的表征稳定性。由此，关键点置信度在本文中不再是被动附带的信息，而是作为统一的软可靠性线索贯穿编码、传播、修复、融合与优化目标。",
    32: "图 1 所提关键点置信度引导可靠性建模框架的整体流程。",
    56: "图 2 置信度约束动态图模块与可靠性感知时序校正模块示意。",
    78: "为系统验证所提方法的有效性，本文在 WLASL [1]、MSASL [2]、SLR500 和 NMFs-CSL 四个孤立词手语识别基准上进行评估。",
    79: "整体实验围绕三个问题展开：1）在标准测试条件下，方法是否能够保持具有竞争力的性能；2）在关键点缺失和坐标噪声等退化输入条件下，方法是否能够获得显著鲁棒性收益；3）这些收益是否主要来自可靠性建模本身，而非更大的模型容量或特定融合协议。需要说明的是，跨论文结果比较主要用于定位本文在整体文献中的相对水平，因为不同方法在输入模态、姿态估计器和预处理流程上并不完全一致；真正用于因果归因的公平比较，主要依赖统一主干、统一预处理、统一融合口径和统一受控扰动协议下的结果。",
    82: "表 1 汇总了本文方法与代表性方法在 WLASL 各子集上的标准测试结果。由于表中同时包含骨架、RGB 与多模态方法，且各方法的输入模态和预处理流程并不完全一致，因此这些结果主要用于定位本文在整体文献中的相对位置，而非进行严格的因果比较。在 WLASL100 上，本文方法取得 78.86% 的 P-I 和 79.59% 的 P-C，高于 BEST [11]，但低于 DSTA-SLR [7] 和 NLA-SLR [20]。总体而言，本文在标准测试条件下保持了具有竞争力的性能，但其主要贡献并不在于追求标准测试最优精度。",
    83: "表 1 WLASL 各子集上的标准测试结果比较。",
    85: "表 2 和表 3 给出了 SLR500 与 NMFs-CSL 上的标准测试结果。本文方法在两组中文基准上均高于 ST-GCN [8]，但仍低于 DSTA-SLR [7] 及若干更强方法。因此，这些结果更适合支撑“保持竞争性标准性能”的定位，而不应被解读为本文核心贡献的主要证据。",
    86: "表 2 SLR500 上的标准测试结果比较。",
    87: "表 3 NMFs-CSL 上的标准测试结果比较。",
    89: "为进一步分析各组成模块的作用，本文在 WLASL100 上进行了统一主干下的模块消融实验。基线模型移除全部置信度相关设计；在此基础上，分别引入置信度引导节点编码、置信度约束动态图模块和可靠性感知时序校正模块，并统计关节点单流与四流融合性能。需要强调的是，关节点单流结果用于观察不同模块对单流骨架表征的直接影响，而四流结果用于比较它们在统一融合协议下对整体决策的贡献。",
    90: "表 4 表明，不同模块在单流和融合层面的作用并不完全一致。关节点单流上，baseline、node encoding、graph only、temporal only 和 full model 的 Top-1 分别为 63.42%、65.10%、65.10%、67.28% 和 65.60%，说明三类可靠性建模模块均可带来正向收益，其中时序校正在单流稳定化上最为直接。四流融合方面，node encoding 已能带来最明显的整体提升，而完整模型在质量加权与固定平均两种融合口径下均保持最优或并列最优的 P-I 表现，表明输入质量先验、结构约束与时序修复在多流决策中具有互补关系。",
    91: "表 4 WLASL100 上的模块消融结果与融合控制对比。",
    94: "图 3 不同模块配置在单流与四流结果上的对比。",
    95: "综合表 4 与图 3 可以看出，不同模块的收益具有明显的层次差异。时间校正更直接改善关节点单流表征，节点编码更显著推动多流融合，而完整模型的价值体现在将这些局部增益整合为更稳定的整体判别链路。因此，本文更关注各模块如何围绕统一的可靠性先验协同作用，而不是要求每个局部指标都由完整配置单独达到绝对最优。",
    98: "表 5 结构增强与一致性训练的解耦结果。",
    99: "表 5 用于区分结构增强收益与训练策略收益。在统一主干和统一评估协议下，结构增强版本将无扰动 Top-1 从 63.42% 提升到 65.60%，并在 30% 关键点缺失条件下从 8.39% 提升到 48.49%，表明结构层面的可靠性建模主要改善缺失型退化。进一步加入可靠性感知一致性后，无扰动、30% 缺失、20 像素噪声和低质量样本上的 Top-1 分别提升到 67.95%、59.40%、58.56% 和 63.32%，说明结构模块和训练约束在不同退化类型下承担互补角色。",
    103: "图 4 受控扰动鲁棒性分析设置与结果概览。",
    106: "表 6 关键点缺失扰动下的鲁棒性结果。",
    110: "表 7 坐标噪声扰动下的鲁棒性结果。",
    113: "图 5 不同扰动强度下的性能变化趋势。",
    115: "表 8 统一主干与统一受控扰动协议下的鲁棒性对比。",
    116: "为避免将鲁棒性收益简单归因于主干差异，表 8 在统一 DSTA-SLR [7] 主干和统一受控扰动协议下直接对比了基线模型、结构增强版本和一致性增强版本。结果表明，结构增强版本相对强骨架基线在缺失场景下收益最明显，而一致性增强版本进一步在强噪声和低质量样本上取得更稳定优势。该结果与表 5 的结论一致，即结构可靠性建模主要缓解缺失型退化，训练阶段一致性约束负责补足强噪声场景。",
    121: "综合标准测试比较、统一主干消融、固定平均融合控制、受控扰动对比和开销分析，本文最重要的结论并不是“提出了一个更强的骨架主干”，而是证明了观测可靠性本身可以成为骨架 ISLR 中一个值得单独研究的设计维度。如果仅按标准测试排名衡量，本文并未在所有基准上稳定超过 DSTA-SLR [7] 等强基线；但从问题驱动与证据完整性的角度看，本文仍具有明确价值：它识别了一个真实部署中重要但文献中相对欠缺的问题，并在几乎不增加推理开销的前提下，通过统一协议提供了可归因的鲁棒性收益证据。因此，本文更合理的定位是现有强骨架主干的低开销、可插拔鲁棒性增强组件。",
    123: "尽管上述结果支持本文在统一受控扰动协议下的鲁棒性优势，但仍需明确其适用边界。当前关键点缺失和高斯噪声实验属于受控扰动分析，并不等同于真实部署中的全部退化分布；本文也依赖姿态估计器输出的置信度通道，尚未验证跨姿态估计器和跨置信度分布条件下的一致泛化。此外，若前端姿态估计器以高置信度输出系统性误检，当前框架也无法独立校验该置信度的真伪。",
    125: "本文围绕骨架孤立词手语识别中长期被弱化的“观测可靠性”问题，提出了一个关键点置信度引导的统一可靠性建模框架。不同于将研究重点放在更大的骨架主干或更复杂的时空表示上，本文将关键点置信度视为相对可靠性先验，并将其系统融入输入编码、空间传播、时间修复、多流融合以及训练阶段的一致性约束之中，从而使模型能够在识别链路的多个环节显式抑制不可靠观测带来的负面影响。实验结果表明，所提方法在标准测试条件下保持了具有竞争力的性能；更重要的是，在统一主干与统一受控扰动协议下，该方法在关键点缺失和坐标噪声条件下表现出显著更强的稳定性，并且几乎不增加额外部署开销。这说明，可靠性建模并不是对现有骨架识别主干的简单附属修补，而是一条值得单独研究的、对真实部署更具价值的技术路径。因此，本文更合适的定位不是“提出一个新的通用最强骨架主干”，而是“为现有强骨架识别器提供一个低开销、可插拔且具有明确归因证据的鲁棒性增强模块”。当然，本文仍存在一定边界：当前实验主要基于统一的受控扰动协议来刻画退化观测下的相对鲁棒性，尚不能完全覆盖真实部署中的全部质量退化模式；同时，本文依赖前端姿态估计器输出的置信度信号，尚未系统验证跨姿态估计器与跨置信度分布条件下的一致泛化能力。未来工作可进一步结合真实低质量样本、跨估计器设置以及更显式的不确定性校准策略，对骨架手语识别中的可靠性建模进行更全面的研究。",
}


TABLE4_SINGLE_STREAM = {
    1: "63.42",
    2: "65.10",
    3: "65.10",
    4: "67.28",
    5: "65.60",
}


EXPECTED_MARKERS = {
    8: "引言",
    17: "相关工作",
    26: "方法",
    76: "实验",
    120: "讨论",
    124: "结论",
}


def update_paragraph(doc, index_1based, text):
    paragraph = doc.paragraphs[index_1based - 1]
    if paragraph.runs:
        paragraph.runs[0].text = text
        for run in paragraph.runs[1:]:
            run.text = ""
    else:
        paragraph.text = text


def validate_markers(doc):
    for index_1based, expected in EXPECTED_MARKERS.items():
        actual = doc.paragraphs[index_1based - 1].text.strip()
        if actual != expected:
            raise RuntimeError(
                f"Paragraph {index_1based} marker mismatch: expected '{expected}', got '{actual}'"
            )


def update_table4(doc):
    table = doc.tables[3]
    for row_idx, value in TABLE4_SINGLE_STREAM.items():
        table.rows[row_idx].cells[1].text = value


def main():
    parser = argparse.ArgumentParser(description="Rewrite Chinese WACV-oriented sections in the manuscript DOCX.")
    parser.add_argument("--input", default=str(DEFAULT_INPUT))
    parser.add_argument("--output", default=str(DEFAULT_OUTPUT))
    args = parser.parse_args()

    input_path = Path(args.input)
    output_path = Path(args.output)

    doc = Document(str(input_path))
    validate_markers(doc)
    for index_1based, text in PARAGRAPH_UPDATES.items():
        update_paragraph(doc, index_1based, text)
    update_table4(doc)
    doc.save(str(output_path))
    print(output_path)


if __name__ == "__main__":
    main()
